import {Stack} from 'expo-router'; import {SQLiteProvider} from 'expo-sqlite'; import {StoreProvider} from '../src/Store'; import {initDb} from '../src/db';
export default function Layout(){return <SQLiteProvider databaseName="gestore-spese.db" onInit={initDb}><StoreProvider><Stack screenOptions={{headerShown:false}}><Stack.Screen name="(tabs)"/><Stack.Screen name="expense" options={{presentation:'modal'}}/></Stack></StoreProvider></SQLiteProvider>}
